"""
pipeline — Live processing pipeline for the AI-Enabled Urban Land
Geospatial Integration System.

This package implements the authoritative workflow defined in
CODING_AGENT_MASTER_PROMPT.md.  It is intentionally separate from the
legacy ``engine/`` package (GNSS / star-topology architecture) which is
kept in the repo but NOT wired into this pipeline.
"""
